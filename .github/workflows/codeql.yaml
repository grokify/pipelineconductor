name: CodeQL SAST
on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
  schedule:
    - cron: '30 1 * * 0'
  workflow_dispatch:

jobs:
  codeql:
    uses: grokify/.github/.github/workflows/go-codeql.yaml@main
